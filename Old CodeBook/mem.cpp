//mem
#include <cstdio>
#include <cstring>

memset(s,0,sizeof(s));
memcmp(s1,s2,sizeof(s2)); // 1 / 0 / -1
memcpy(&s1,&s2,sizeof(s2)); // s1,s2 are structs
memcpy(s1,s2,sizeof(s2)); // s1,s2 are strings